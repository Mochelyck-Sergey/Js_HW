1. 'number' + 3 + 3  // Результат number33. Происходит сложение, сначала получится number3 + 3, number33.
2. null + 3 // Результат 3. null равен 0 + 3 равно 3. 
3. 5 && "qwerty" // Результат qwerty. Логическое "и" показывает первое ложное или последнее правдивое значение.
4. +'40' + +'2' + "hillel" // Результат 42hillel. Плюс(унарный) превращает строки в числа 40+2 и дальше как в 1 примере, 42+hillel. 
5. '10' - 5 === 6 // Результат false. 10 - 5 = 5 и строгое равно к 6.  
6. true + false // Результат 1. false=0,true=1 сложение. 
7. '4px' - 3 // Результат NaN. 4px нельзя превратить в чило. 
8. '4' - 3 // Результат 1. 4 чило - 3. 
9. '6' + 3 ** 0 // Результат 61. Сначала степень, любое число в нулевой степени 1, + строка 6.
10. 12 / '6' // Результат 2. 6 переводим в число, 12 делим на 6.
11. '10' + (5 === 6)// Результат 10false. 5 не равно 6 и получаем false, складываем с строкой. 
12. null == ''// Результат false. null не равен пустой строке. 
13. 3 ** (9 / 3) // Результат 27. Сначала делим, потом возводим 3 в третью степень.
14 !!'false' == !!'true' // Результат true. Забываем, что внутри строки (главное, что не пустое). !'что то'=false (строка не пустая) и !false=true, так же само и с 2 частью true == true получим true 
15. 0 || '0' && 1. // Результат 1. Логический оператор И имеет более высокий приоритет, с первой части мы возвращаем 1 т.к. оба true. Теперь 0 || 1, 0 это ложное значение, поэтому 1.
16.(+null == false) < 1 // Результат false. Приведем null к числу(+null=0). Дальше приведем к одному типу false к числу 0, 0==0 будет true. True < 1, приводим тру к числу =1, 1<1 = false.
17. false && true || true // Результат true. Оператор && больший приоритет (первое ложное или последнее истинное) получаем false. || (первое истинное или последнее ложное) false||true, получаем true.
18. false && (false || true) // Результат false. В этом примере сначала в скобках. Из скобок берем true. false && true, берем первое ложное.
19. (+null == false) < 1 ** 5 // Результат false. Возводим в степень, 1 в 5 =1. +null равно 0. false приводим к 0, получаем 0==0 равно true. True < 1, true приводим к 1 и получаем 1<1, а это false.